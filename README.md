# StrumGen - Конструктор боя для гитары

Интерактивный инструмент для создания, визуализации и прослушивания гитарных боёв с синхронизацией звука и визуальных подсказок.

## 🎵 Описание

StrumGen позволяет создавать и настраивать гитарные бои для ваших любимых песен. Вы можете вводить аккорды, настраивать темп, генерировать случайные последовательности ударов и экспортировать результаты для дальнейшего использования.

## 🚀 Основные возможности

### 🎸 Генерация боёв
- Поддержка различных темпов (4, 8, 16 долей)
- Визуальное редактирование стрелок (вверх/вниз)
- Контроль звука через кружки переключения
- Случайная генерация боёв одной кнопкой

### 🎵 Аудио синтез
- Реалистичный синтез звука гитары с использованием Web Audio API
- Поддержка аккордовых прогрессий с автоматическим распределением по тактам
- Инверсии аккордов для разнообразного звучания
- Синхронизация визуальных подсветок с аудио

### 📱 Адаптивный интерфейс
- Полная поддержка мобильных устройств
- Сенсорная оптимизация для удобного управления
- Адаптивная верстка под разные размеры экранов

### 🎼 Система шаблонов
- **Автоматическое обнаружение**: новые шаблоны добавляются просто помещением JSON файла в папку `templates/`
- **Автоматическая генерация**: при сборке проекта manifest.json создаётся автоматически
- **Гибкая система**: поддержка любых кастомных шаблонов с индивидуальными настройками
- **Предустановленные шаблоны**: Бой Пятёрка, Бой Восьмёрка, Рок и др.

### 💾 Импорт/Экспорт
- Экспорт в PNG для визуального сохранения боёв
- Импорт/Экспорт в JSON для сохранения и обмена данными
- Локальное хранение данных в браузере

## 🛠️ Технологический стек

- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Стилизация**: Tailwind CSS
- **Аудио**: Web Audio API с модульной архитектурой
- **Сборка**: Webpack, Babel
- **Экспорт**: html2canvas

## 🏗️ Архитектура аудиосистемы

### Недавние улучшения (рефакторинг Metronome.js)

Проект был успешно рефакторирован для улучшения читаемости, переиспользования кода и тестируемости путем разделения аудиосистемы на модули:

#### 📁 Созданные модули

**`js/audio/AudioManager.js`** - Управление AudioContext
- Отвечает за создание и инициализацию AudioContext
- Управляет состоянием аудио (suspended/running)
- Предоставляет функционал возобновления для мобильных браузеров
- Экспортирует чистый API для проверки состояния аудио

**`js/audio/AudioEngine.js`** - Движок синтеза звука
- Содержит метод `scheduleClick()` для метрономных тиков
- Содержит метод `createGuitarSound()` для синтеза гитарного звука
- Обрабатывает fallback-аудио для неподдерживаемых браузеров
- Использует паттерн dependency injection с AudioManager

#### 🔄 Изменения в Metronome.js

**Удалено прямое управление AudioContext:**
```javascript
// Было:
this.audioCtx = AudioPolyfill.createAudioContext();

// Стало:
this.audioManager = new AudioManager();
this.audioEngine = new AudioEngine(this.audioManager);
```

**Заменены вызовы методов синтеза звука:**
```javascript
// Было:
this.createGuitarSound(freq, 0.25, volume);

// Стало:
this.audioEngine.createGuitarSound(freq, 0.25, volume);
```

**Разбита большая функция scheduler() на меньшие методы:**
- `checkAudioState()` - валидация готовности аудио
- `scheduleBeat()` - планирование отдельных битов
- `scheduleArrow()` - планирование звуков для стрелок

#### ✅ Преимущества новой архитектуры

- **Улучшенная читаемость** - код организован по ответственности
- **Переиспользование кода** - AudioManager и AudioEngine могут использоваться в других компонентах
- **Легче тестировать** - каждый модуль имеет сфокусированные обязанности
- **Уменьшенная сложность** - Metronome.js стал чище и удобнее для поддержки
- **Правильное разделение ответственности** - аудио-логика отделена от логики метронома

## 📁 Структура проекта

```
guitar-combat-restore/
├── index.html              # Главная страница приложения
├── js/                     # Исходный код JavaScript
│   ├── main.js            # Точка входа приложения
│   ├── components/        # Компоненты интерфейса
│   │   ├── BeatRow.js     # Компонент строки битов
│   │   ├── Controls.js    # Компонент управления
│   │   ├── Playback.js    # Компонент воспроизведения
│   │   ├── Metronome.js   # Компонент метронома (рефакторирован для модульной архитектуры)
│   │   ├── ChordManager.js # Управление аккордами
│   │   ├── MobileMenu.js  # Мобильное меню
│   │   └── Modal.js       # Модальное окно
│   ├── audio/             # Модули аудиосистемы (новые после рефакторинга)
│   │   ├── AudioManager.js # Управление AudioContext
│   │   └── AudioEngine.js  # Синтез звука и планирование
│   └── utils/             # Вспомогательные утилиты
│       ├── AudioPolyfill.js # Полифилы для Web Audio API
│       ├── ExportUtils.js # Утилиты экспорта данных
│       ├── ImportUtils.js # Утилиты импорта данных
│       └── TemplateScanner.js # Сканер шаблонов (упрощённая версия)
├── css/                   # Стили
│   ├── input.css          # Входной файл Tailwind
│   └── styles.css         # Сгенерированные стили
├── templates/             # Шаблоны боёв (автоматически сканируются)
│   ├── manifest.json      # Автоматически генерируется при сборке
│   ├── Бой Пятёрка.json   # Предустановленный шаблон
│   ├── Бой Восьмёрка.json # Предустановленный шаблон
│   └── рок.json           # Предустановленный шаблон
├── scripts/               # Скрипты сборки
│   └── generate-manifest.js # Генератор manifest.json из шаблонов
├── dist/                  # Собранный проект
├── package.json           # Зависимости и скрипты
├── webpack.config.js      # Конфигурация Webpack
├── tailwind.config.js     # Конфигурация Tailwind
└── README.md              # Документация
```

## 📋 Установка и запуск

### Требования
- Node.js (версия 14 или выше)
- npm (обычно идет вместе с Node.js)

### Установка зависимостей

```bash
npm install
```

### Разработка

Для запуска в режиме разработки с горячей перезагрузкой:

```bash
npm run dev
```

### Сборка проекта

Для создания оптимизированной сборки:

```bash
npm run build
```

### Запуск без сервера

После сборки проект можно запустить напрямую из файла:

1. Откройте файл `dist/index.html` в браузере напрямую (через file:// протокол)

### Запуск с локальным сервером

```bash
npm start
```

Сервер будет доступен по адресу: http://localhost:9000

## 🎯 Использование

### Основные элементы интерфейса

1. **Поле ввода аккордов** - введите аккорды через пробел (например: "Am F Dm E")
2. **Визуализация боя** - сетка стрелок с кружками переключения
3. **Кнопка воспроизведения** - запуск/остановка метронома
4. **Панель управления**:
   - Темп (BPM) - регулировка скорости
   - Количество стрелок - выбор сложности
   - Шаблоны - предустановленные бои
   - Кнопки экспорта/импорта

### Работа с боем

- **Стрелки** - направление удара (вверх/вниз)
- **Кружки** - включение/выключение звука для конкретной позиции
- **Клик по стрелке** - установка текущей позиции воспроизведения
- **Клик по кружку** - включение/выключение звука

### Аккорды

Поддерживаются следующие аккорды:
- Am, A, Dm, D, Em, E, G, C, F, B, Bm, F#m

Аккорды автоматически распределяются по тактам в соответствии с прогрессией.

## 📱 Мобильная версия

Приложение полностью адаптировано для мобильных устройств:
- Специальное меню для навигации
- Увеличенные области касания
- Оптимизированные размеры элементов

## 🎼 Разработка шаблонов

### Добавление новых шаблонов

1. **Создайте JSON файл** с описанием шаблона в папке `templates/`:
   ```json
   {
     "description": "Описание вашего шаблона боя",
     "bpm": 120,
     "count": 8,
     "beats": [
       {"direction": "down", "play": true},
       {"direction": "up", "play": false},
       {"direction": "down", "play": true},
       {"direction": "up", "play": true}
     ]
   }
   ```

2. **Запустите сборку проекта**:
   ```bash
   npm run build
   ```

3. **Готово!** Новый шаблон автоматически появится в выпадающем списке

### Структура JSON шаблона

- `description` - описание шаблона (опционально)
- `bpm` - темп в ударах в минуту (опционально)
- `count` - количество ударов (опционально)
- `beats` - массив ударов с параметрами:
  - `direction`: `"up"` или `"down"`
  - `play`: `true`/`false` - проигрывать ли звук
- `chords` - массив аккордов (опционально)

### Автоматическая генерация

Система автоматически:
- Генерирует человеко-читаемое имя из названия файла
- Создаёт уникальный ID
- Определяет количество ударов из массива `beats`
- Добавляет шаблон в `manifest.json`

## 🔧 Скрипты

- `npm run build:css` - сборка CSS из Tailwind
- `npm run watch:css` - отслеживание изменений CSS
- `npm run dev` - режим разработки
- `npm run build` - сборка для продакшена (автоматически генерирует manifest.json)
- `npm run generate-manifest` - ручная генерация manifest.json из шаблонов
- `npm run docs` - генерация JSDoc документации в папку docs/
- `npm start` - запуск локального сервера

### 📖 Документация API

Полную документацию API можно найти в сгенерированной JSDoc документации:

1. Запустите генерацию: `npm run docs`
2. Откройте `docs/index.html` в браузере

Документация включает:
- Подробное описание всех классов и методов
- Типы параметров и возвращаемых значений
- Примеры использования
- Интерактивную навигацию по коду

## ✅ Недавние улучшения

### Завершенные задачи по рефакторингу Metronome.js

- ✅ **Анализ структуры Metronome.js** - изучена текущая архитектура
- ✅ **Создание директории js/audio/** - подготовлена структура для аудиомодулей
- ✅ **Создание AudioManager.js** - реализован менеджер AudioContext
- ✅ **Создание AudioEngine.js** - реализован движок синтеза звука
- ✅ **Рефакторинг Metronome.js** - интеграция новых модулей
- ✅ **Разбиение scheduler()** - декомпозиция на меньшие методы
- ✅ **Тестирование функциональности** - успешная сборка проекта

Все запланированные улучшения аудиоархитектуры успешно реализованы с сохранением полной функциональности приложения.

## 🌐 Интеграция с Telegram

Приложение поддерживает работу в Telegram WebApp, что позволяет использовать его напрямую в Telegram-ботах.

## 📄 Лицензия

MIT License - вы можете свободно использовать, модифицировать и распространять это приложение.

## 📞 Контакты

Если у вас есть вопросы или предложения, вы можете связаться с разработчиком по адресу: dmitriydzhioev@gmail.com

## 🔒 Безопасность и конфиденциальность

Все данные хранятся локально в браузере пользователя и не передаются на сервер. Приложение не собирает персональную информацию и использует файлы cookie только для улучшения пользовательского опыта.

Подробную информацию о политике конфиденциальности и условиях использования можно найти в соответствующих разделах приложения.
