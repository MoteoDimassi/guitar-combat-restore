# StrumGen - Конструктор боя для гитары

Интерактивный инструмент для создания, визуализации и прослушивания гитарных боёв с синхронизацией звука и визуальных подсказок.

## 🎵 Описание

StrumGen позволяет создавать и настраивать гитарные бои для ваших любимых песен. Вы можете вводить аккорды, настраивать темп, генерировать случайные последовательности ударов и экспортировать результаты для дальнейшего использования.

## 🚀 Основные возможности

### 🎸 Генерация боёв
- Поддержка различных темпов (4, 8, 16 долей)
- Визуальное редактирование стрелок (вверх/вниз)
- Контроль звука через кружки переключения
- Случайная генерация боёв одной кнопкой

### 🎵 Аудио синтез
- Реалистичный синтез звука гитары с использованием Web Audio API
- Поддержка аккордовых прогрессий с автоматическим распределением по тактам
- Инверсии аккордов для разнообразного звучания
- Синхронизация визуальных подсветок с аудио

### 📱 Адаптивный интерфейс
- Полная поддержка мобильных устройств
- Сенсорная оптимизация для удобного управления
- Адаптивная верстка под разные размеры экранов

### 🎼 Система шаблонов
- **Автоматическое обнаружение**: новые шаблоны добавляются просто помещением JSON файла в папку `templates/`
- **Автоматическая генерация**: при сборке проекта manifest.json создаётся автоматически
- **Гибкая система**: поддержка любых кастомных шаблонов с индивидуальными настройками
- **Предустановленные шаблоны**: Бой Пятёрка, Бой Восьмёрка, Рок и др.

### 💾 Импорт/Экспорт
- Экспорт в PNG для визуального сохранения боёв
- Импорт/Экспорт в JSON для сохранения и обмена данными
- Локальное хранение данных в браузере

## 🛠️ Технологический стек

- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Стилизация**: Tailwind CSS
- **Аудио**: Web Audio API с модульной архитектурой
- **Сборка**: Webpack, Babel
- **Экспорт**: html2canvas
- **Документация**: JSDoc

## 🏗️ Архитектура проекта

Проект построен на модульной архитектуре с разделением ответственности:

### 📁 Структура проекта

```
guitar-combat-restore/
├── index.html              # Главная страница приложения
├── songs.html              # Страница с песнями
├── js/                     # Исходный код JavaScript
│   ├── main.js            # Точка входа приложения
│   ├── components/        # Компоненты интерфейса
│   │   ├── BeatRow.js     # Компонент строки битов
│   │   ├── Controls.js    # Компонент управления
│   │   ├── Playback.js    # Компонент воспроизведения
│   │   ├── Metronome.js   # Компонент метронома с аудио-движком
│   │   ├── ChordManager.js # Управление аккордами
│   │   ├── ChordDisplay.js # Отображение текущих аккордов
│   │   ├── MobileMenu.js  # Мобильное меню
│   │   ├── Modal.js       # Модальное окно
│   │   └── TemplateManager.js # Управление шаблонами
│   ├── audio/             # Модули аудиосистемы
│   │   ├── AudioManager.js # Управление AudioContext
│   │   └── AudioEngine.js  # Синтез звука
│   └── utils/             # Вспомогательные утилиты
│       ├── AudioPolyfill.js # Полифилы для Web Audio API
│       ├── ExportUtils.js # Утилиты экспорта данных
│       ├── ImportUtils.js # Утилиты импорта данных
│       └── TemplateScanner.js # Сканер шаблонов
├── css/                   # Стили
│   ├── input.css          # Входной файл Tailwind
│   └── styles.css         # Сгенерированные стили
├── templates/             # Шаблоны боёв
│   ├── manifest.json      # Автоматически генерируется
│   ├── Бой Пятёрка.json   # Предустановленный шаблон
│   ├── Бой Восьмёрка.json # Предустановленный шаблон
│   └── рок.json           # Предустановленный шаблон
├── scripts/               # Скрипты сборки
│   └── generate-manifest.js # Генератор manifest.json
├── docs/                  # Сгенерированная документация (после npm run docs)
├── package.json           # Зависимости и скрипты
├── webpack.config.js      # Конфигурация Webpack
├── tailwind.config.js     # Конфигурация Tailwind
└── README.md              # Документация
```

### 🔧 Архитектура аудиосистемы

Аудиосистема построена на модульной архитектуре с разделением ответственности:

#### AudioManager (`js/audio/AudioManager.js`)
- Управляет AudioContext
- Обрабатывает инициализацию и состояние аудио
- Предоставляет методы для возобновления аудио на мобильных устройствах
- Инкапсулирует всю работу с Web Audio API

#### AudioEngine (`js/audio/AudioEngine.js`)
- Отвечает за синтез звука
- Создает метрономные клики и гитарные звуки
- Использует ADSR огибающие и фильтры для реалистичного звучания
- Поддерживает fallback-аудио для неподдерживаемых браузеров

#### Metronome (`js/components/Metronome.js`)
- Координирует аудио и визуализацию
- Управляет планированием нот и подсветкой
- Обрабатывает аккордовые прогрессии
- Синхронизирует визуальные и аудио события

## 📋 Установка и запуск

### Требования
- Node.js (версия 14 или выше)
- npm (обычно идет вместе с Node.js)

### Установка зависимостей

```bash
npm install
```

### Режимы запуска

#### Разработка с горячей перезагрузкой

```bash
npm run dev
```

#### Сборка для продакшена

```bash
npm run build
```

#### Запуск локального сервера

```bash
npm start
```

Сервер будет доступен по адресу: http://localhost:9000

### Запуск без сервера

После сборки проект можно запустить напрямую из файла:

1. Откройте файл `dist/index.html` в браузере

## ⚙️ Настройка

### Конфигурация Webpack

Основные настройки в `webpack.config.js`:
- Entry point: `js/main.js`
- Output: `dist/bundle.js`
- Поддержка ES6+ через Babel
- Автоматическое копирование статических файлов

### Конфигурация Tailwind CSS

Настройки в `tailwind.config.js`:
- Content paths: HTML и JS файлы
- Темы: кастомные цвета и шрифты

### Переменные окружения

Проект не использует переменные окружения. Все настройки задаются в конфигурационных файлах.

## 🎯 Использование

### Основные элементы интерфейса

1. **Поле ввода аккордов** - введите аккорды через пробел (например: "Am F Dm E")
2. **Визуализация боя** - сетка стрелок с кружками переключения
3. **Кнопка воспроизведения** - запуск/остановка метронома
4. **Панель управления**:
   - Темп (BPM) - регулировка скорости (40-200)
   - Количество стрелок - выбор сложности (4, 8, 16)
   - Шаблоны - предустановленные бои
   - Кнопки экспорта/импорта

### Работа с боем

- **Стрелки** - направление удара (вверх/вниз)
- **Кружки** - включение/выключение звука для конкретной позиции
- **Клик по стрелке** - переключение направления
- **Клик по кружку** - включение/выключение звука

### Аккорды

Поддерживаемые аккорды:
- Основные: Am, A, Dm, D, Em, E, G, C, F, B
- С септимой: Bm, F#m

Аккорды автоматически распределяются по тактам в соответствии с прогрессией.

### Мобильная версия

Приложение полностью адаптировано для мобильных устройств:
- Специальное меню для навигации
- Увеличенные области касания
- Оптимизированные размеры элементов
- Поддержка Telegram WebApp

## 📖 API документация

### Основные классы

#### Metronome
```javascript
const metronome = new Metronome();

// Запуск воспроизведения
await metronome.start();

// Остановка
metronome.stop();

// Установка темпа
metronome.setBpm(120);

// Установка количества стрелок
metronome.setBeatCount(8);

// Обновление аккордов
metronome.updateChords("Am F C G");
```

#### AudioManager
```javascript
const audioManager = new AudioManager();

// Инициализация
await audioManager.initialize();

// Проверка готовности
if (audioManager.isReady()) {
  // Аудио готово к использованию
}

// Возобновление (для мобильных)
await audioManager.resume();
```

#### AudioEngine
```javascript
const audioEngine = new AudioEngine(audioManager);

// Создание метрономного клика
audioEngine.scheduleClick(currentTime, isAccent);

// Синтез гитарного звука
audioEngine.createGuitarSound(frequency, duration, volume);
```

### События

Приложение использует собственную систему событий для коммуникации между компонентами:

- `DOMContentLoaded` - инициализация приложения
- `visibilitychange` - обработка фокуса окна
- `resize` - адаптация интерфейса
- `input` - обновление аккордов в реальном времени

## 💡 Примеры

### Создание простого боя

```javascript
// Инициализация компонентов
const beatRow = new BeatRow();
const controls = new Controls(beatRow);
const metronome = new Metronome();

// Настройка боя
controls.setCount(8); // 8 стрелок
metronome.setBpm(120); // 120 BPM
metronome.updateChords("Am F C G"); // Аккордовая прогрессия

// Запуск
await metronome.start();
```

### Импорт/Экспорт данных

```javascript
// Экспорт в JSON
const exportUtils = new ExportUtils(beatRow);
exportUtils.downloadJSON();

// Импорт из файла
const importUtils = new ImportUtils(beatRow);
// Обработка выбора файла происходит автоматически через UI
```

### Работа с шаблонами

```javascript
const templateManager = new TemplateManager(beatRow, controls);

// Загрузка шаблона
templateManager.loadTemplate('бой-восьмёрка');

// Применение шаблона к интерфейсу
templateManager.applyTemplate(templateData);
```

## 🧪 Тестирование

### Запуск тестов

```bash
npm test
```

### Ручное тестирование

1. **Функциональное тестирование**:
   - Проверка создания и воспроизведения боёв
   - Тестирование импорта/экспорта
   - Валидация работы с различными браузерами

2. **Кросс-браузерное тестирование**:
   - Chrome/Chromium
   - Firefox
   - Safari
   - Edge

3. **Мобильное тестирование**:
   - iOS Safari
   - Chrome Mobile
   - Firefox Mobile

### Отладка

Используйте Developer Tools браузера для:
- Мониторинга состояния AudioContext
- Проверки сетевых запросов
- Анализа производительности

## 🔧 Скрипты

- `npm run build:css` - сборка CSS из Tailwind
- `npm run watch:css` - отслеживание изменений CSS
- `npm run dev` - режим разработки
- `npm run build` - сборка для продакшена с генерацией manifest
- `npm run generate-manifest` - ручная генерация manifest.json
- `npm run docs` - генерация JSDoc документации
- `npm start` - запуск локального сервера

## 🎼 Разработка шаблонов

### Добавление новых шаблонов

1. **Создайте JSON файл** в папке `templates/`:
   ```json
   {
     "beats": [
       {"direction": "down", "play": true},
       {"direction": "up", "play": false}
     ],
     "bpm": 120,
     "speed": 100,
     "chords": ["Am F C G"],
     "timestamp": "2025-01-01T00:00:00.000Z"
   }
   ```

2. **Запустите сборку проекта**:
   ```bash
   npm run build
   ```

3. **Готово!** Новый шаблон появится в выпадающем списке

### Структура JSON шаблона

- `beats` - массив ударов с `direction` и `play`
- `bpm` - темп в ударах в минуту
- `speed` - скорость воспроизведения
- `chords` - массив аккордовых прогрессий
- `timestamp` - время создания

## 🚨 Решение проблем

### Аудио не работает

**Проблема**: Звук не воспроизводится в браузере

**Решения**:
1. Проверьте настройки звука браузера
2. Попробуйте обновить страницу
3. Проверьте поддержку Web Audio API
4. На мобильных устройствах: убедитесь, что звук включен

### Приложение не загружается

**Проблема**: Белый экран или ошибки JavaScript

**Решения**:
1. Очистите кэш браузера
2. Проверьте консоль на ошибки
3. Убедитесь, что все файлы собраны (`npm run build`)
4. Проверьте поддержку ES6 модулей

### Проблемы с мобильными устройствами

**Проблема**: Не работает сенсорное управление

**Решения**:
1. Обновите браузер до последней версии
2. Проверьте настройки приватности
3. Попробуйте другой браузер (Chrome, Safari)

### Экспорт не работает

**Проблема**: Не удается экспортировать в PNG

**Решения**:
1. Проверьте поддержку html2canvas
2. Попробуйте другой браузер
3. Убедитесь, что контент полностью загружен

## 🔄 Интеграция с Telegram

Приложение поддерживает работу в Telegram WebApp:

```javascript
// Получение объекта Telegram WebApp
const tg = window.Telegram.WebApp;

// Разворачивание на весь экран
tg.expand();

// Отправка данных в бота
tg.sendData(JSON.stringify(data));
```

## 📄 Лицензия

MIT License - вы можете свободно использовать, модифицировать и распространять это приложение.

## 📞 Контакты

Если у вас есть вопросы или предложения, вы можете связаться с разработчиком по адресу: dmitriydzhioev@gmail.com

## 🔒 Безопасность и конфиденциальность

Все данные хранятся локально в браузере пользователя и не передаются на сервер. Приложение не собирает персональную информацию и использует файлы cookie только для улучшения пользовательского опыта.

Подробную информацию о политике конфиденциальности и условиях использования можно найти в соответствующих разделах приложения.</result>
<line_count>313</line_count>
</write_to_file>
