# Guitar Combat - Конструктор боя для гитары

## Описание проекта

Guitar Combat - это современное веб-приложение для создания и визуализации гитарных боев. Позволяет создавать интерактивные уроки с аккордами, ритмом, текстами песен и визуальными стрелочками для обучения игре на гитаре. Приложение построено на модульной архитектуре с четким разделением логики, представления и данных.

## Возможности

- 🎸 **Визуализация гитарного боя** - интерактивные стрелочки с чередующимися направлениями
- 🎵 **Работа с аккордами** - поддержка сложных аккордов и их парсинг
- 📝 **Тексты песен** - синхронизация слогов с ритмом боя
- 🎼 **Готовые шаблоны** - предустановленные паттерны боев (Пятерка, Восьмерка, Шестерка)
- 🎚️ **Настройки темпа** - регулировка BPM и количества стрелочек
- 💾 **Сохранение/загрузка** - экспорт и импорт настроек в JSON
- 📱 **Адаптивный дизайн** - работает на всех устройствах
- 🎨 **Современный UI** - темная тема с акцентными цветами

## Архитектура проекта

### Структура папок

```
├── css/            # Стили приложения
│   ├── input.css   # Исходные стили Tailwind
│   └── styles.css  # Скомпилированные стили
├── dist/           # Собранная версия приложения
├── docs/           # Документация (генерируется JSDoc)
├── js/             # Основная логика приложения
│   ├── main.js     # Главный файл приложения (GuitarCombatApp)
│   ├── Audio/      # Аудио компоненты (планируется)
│   ├── Chords/     # Логика работы с аккордами
│   │   ├── ChordBuilder.js    # Создание аккордов из названий
│   │   └── ChordParser.js     # Парсинг аккордов из поля ввода
│   ├── Measure/    # Музыкальные такты и длительности
│   │   ├── Bar.js             # Основной класс такта
│   │   ├── BeatUnit.js        # Длительность в такте
│   │   ├── ChordChange.js     # Смена аккорда
│   │   └── LyricSyllable.js   # Слог в тексте песни
│   ├── ModalWindows/   # Модальные окна
│   │   ├── Modal.js           # Базовый класс модального окна
│   │   ├── PrivacyPolicyModal.js # Политика конфиденциальности
│   │   └── TermsOfUseModal.js # Условия использования
│   ├── Strum/      # Компоненты для отображения боя
│   │   └── ArrowDisplay.js    # Отображение стрелочек боя
│   └── View/       # Компоненты представления
│       └── BarDisplay.js      # Отображение тактов на экране
├── templates/      # Шаблоны боев
│   ├── manifest.json           # Манифест шаблонов
│   ├── Пятерка.json           # Шаблон "Пятерка"
│   ├── Восьмерка.json         # Шаблон "Восьмерка"
│   ├── Шестерка.json          # Шаблон "Шестерка"
│   └── ...                    # Другие шаблоны
├── scripts/        # Скрипты сборки
│   └── generate-manifest.js   # Генератор манифеста шаблонов
├── index.html      # Главная страница
├── songs.html      # Страница с песнями
├── package.json    # Зависимости и скрипты
├── webpack.config.js # Конфигурация сборки
└── tailwind.config.js # Конфигурация Tailwind CSS
```

## Основные компоненты

### 1. Система аккордов (js/Chords/)

#### ChordBuilder.js
**Назначение:** Создание аккордов из названий и определение нот.

**Основные возможности:**
- Парсинг названий аккордов (например, "Cmaj7", "Dm/G")
- Вычисление частот нот на основе интервалов
- Поддержка различных типов аккордов:
  - Основные: мажор, минор, уменьшенный, увеличенный
  - Суспенды: sus2, sus4
  - Септаккорды: 7, maj7, m7, dim7, m7b5, 7sus4
  - Расширенные: 9, maj9, m9, 11, 13
  - Сексты: 6, m6

**Ключевые методы:**
- `parseChordName(chordName)` - парсинг названия
- `getChordNotes(chordName)` - получение частот нот
- `isValidChord(chordName)` - проверка валидности
- `calculateChordFrequencies(root, type, bass)` - вычисление частот

#### ChordParser.js
**Назначение:** Парсинг аккордов из поля ввода и создание последовательностей.

**Основные возможности:**
- Парсинг строки аккордов ("Am F C G")
- Валидация названий через ChordBuilder
- Создание карт смен аккордов для тактов
- Поддержка правил смены внутри такта
- Автоматическое зацикливание при недостатке аккордов

**Ключевые методы:**
- `parseChords(chordsString)` - парсинг строки
- `generateChordMaps(beatCount, chordChanges)` - создание карт
- `getChordForPosition(barIndex, beatIndex)` - получение аккорда для позиции
- `updateChords(chordsString, beatCount, chordChanges)` - полное обновление

### 2. Система тактов (js/Measure/)

#### BeatUnit.js
**Назначение:** Представление одной длительности в такте.

**Структура:**
- `index` - индекс длительности в такте
- `type` - тип звукоизвлечения:
  - 0 - не играть
  - 1 - играть
  - 2 - звук с приглушиванием

**Методы:**
- `isPlayed()`, `isMuted()`, `isSkipped()` - проверки состояния
- `setType(type)` - установка типа
- `getTypeString()` - строковое представление

#### ChordChange.js
**Назначение:** Представление смены аккорда в такте.

**Структура:**
- `name` - название аккорда ('Am', 'F', 'C')
- `startBeat` - начало (индекс BeatUnit)
- `endBeat` - конец (индекс BeatUnit)

**Методы:**
- `isActiveAt(beatIndex)` - проверка активности
- `overlapsWith(other)` - проверка пересечений
- `getDuration()` - длительность аккорда
- `shift(offset)` - сдвиг позиции

#### LyricSyllable.js
**Назначение:** Представление слога в тексте песни.

**Структура:**
- `text` - текст слога ("люб-")
- `startBeat` - начало слога
- `duration` - длительность слога

**Методы:**
- `isActiveAt(beatIndex)` - проверка активности
- `getCleanText()` - очищенный текст
- `isEmpty()` - проверка на пустоту

#### Bar.js
**Назначение:** Объединение длительностей, аккордов и слогов в такт.

**Основные возможности:**
- Управление массивом BeatUnit
- Управление массивом ChordChange
- Управление массивом LyricSyllable
- Проверка конфликтов между элементами
- Сортировка и организация элементов

**Ключевые методы:**
- `setBeatType(beatIndex, type)` - установка типа длительности
- `addChordChange(chordName, startBeat, endBeat)` - добавление аккорда
- `addLyricSyllable(text, startBeat, duration)` - добавление слога
- `getChordForBeat(beatIndex)` - получение аккорда для доли
- `getSyllableForBeat(beatIndex)` - получение слога для доли
- `checkConflicts()` - проверка пересечений

### 3. Система представления (js/View/)

#### BarDisplay.js
**Назначение:** Отображение тактов на экране и управление навигацией.

**Основные возможности:**
- Отображение содержимого текущего такта
- Навигация между тактами (nextLineBtn, prevLineBtn)
- Воспроизведение с автоматическим переключением
- Цветовая индикация типов длительностей
- Отображение аккордов и слогов

**Ключевые методы:**
- `init(containerSelector, infoSelector)` - инициализация
- `setBars(bars)` - установка массива тактов
- `nextBar()`, `prevBar()`, `goToBar(index)` - навигация
- `togglePlayback()` - управление воспроизведением
- `updateDisplay()` - обновление отображения

### 4. Модальные окна (js/ModalWindows/)

#### Modal.js
**Назначение:** Базовый класс модальных окон.

**Возможности:**
- Создание HTML структуры
- Привязка событий (ESC, клик по оверлею)
- Анимации открытия/закрытия
- Предотвращение скролла страницы

#### PrivacyPolicyModal.js
**Назначение:** Отображение политики конфиденциальности.

#### TermsOfUseModal.js
**Назначение:** Отображение условий использования.

### 5. Система отображения боя (js/Strum/)

#### ArrowDisplay.js
**Назначение:** Отображение интерактивных стрелочек для визуализации гитарного боя.

**Основные возможности:**
- Генерация стрелочек с чередующимися направлениями (вниз-вверх)
- Настройка количества стрелочек (4, 8, 16)
- Адаптивная раскладка: grid для малого количества, flexbox с прокруткой для большого
- Интерактивность: клик для активации/деактивации стрелочек
- Анимации и подсветка активных стрелочек
- Настраиваемые размеры и отступы

**Ключевые методы:**
- `init(containerSelector, countSelectSelector)` - инициализация
- `setArrowCount(count)` - установка количества стрелочек
- `generateArrows()` - генерация массива стрелочек
- `updateDisplay()` - обновление отображения
- `highlightArrow(index, highlight)` - подсветка стрелочки
- `getActiveArrows()` - получение активных стрелочек

### 6. Главное приложение (js/main.js)

#### GuitarCombatApp
**Назначение:** Центральный класс, объединяющий все компоненты приложения.

**Основные возможности:**
- Инициализация всех компонентов
- Управление состоянием приложения
- Синхронизация между компонентами
- Сохранение и загрузка данных в localStorage
- Обработка событий пользователя

**Ключевые методы:**
- `init()` - инициализация приложения
- `handleChordsInputChange(chordsString)` - обработка ввода аккордов
- `handleBeatCountChange(beatCount)` - обработка изменения количества стрелочек
- `updateDisplay()` - обновление интерфейса
- `saveData()`, `loadSavedData()` - сохранение/загрузка данных

### 7. Шаблоны боев (templates/)

**Назначение:** Предустановленные паттерны гитарных боев для быстрого старта.

**Доступные шаблоны:**
- **Пятерка** - классический паттерн на 8 долей
- **Восьмерка** - паттерн на 16 долей
- **Шестерка** - альтернативный паттерн на 8 долей
- **Варианты с глушением** - модификации с приглушенными ударами

**Структура шаблона:**
```json
{
  "name": "Пятерка",
  "bpm": 90,
  "count": 8,
  "beats": [1, 0, 1, 0, 1, 0, 1, 0],
  "description": "Классический паттерн пятерка"
}
```

### 8. Система сборки

**Webpack конфигурация:**
- Сборка JavaScript модулей с Babel
- Обработка CSS с Tailwind
- Копирование статических файлов
- Dev server для разработки

**Скрипты сборки:**
- `npm run dev` - разработка с автоперезагрузкой CSS
- `npm run build` - сборка для продакшена
- `npm run generate-manifest` - генерация манифеста шаблонов
- `npm run docs` - генерация документации JSDoc

## Логика работы приложения

### 1. Инициализация
1. Создается экземпляр ChordParser
2. Инициализируется BarDisplay с привязкой к DOM элементам
3. Создаются модальные окна для документов

### 2. Ввод аккордов
1. Пользователь вводит строку аккордов в поле chordsInput
2. ChordParser парсит строку и валидирует названия
3. ChordBuilder создает объекты аккордов с нотами
4. Генерируются карты смен аккордов для тактов

### 3. Создание тактов
1. Создаются объекты Bar для каждого такта
2. В каждый Bar добавляются BeatUnit (длительности)
3. Настраиваются типы звукоизвлечения для каждой доли
4. Добавляются ChordChange для смен аккордов
5. Добавляются LyricSyllable для текста песни

### 4. Отображение и навигация
1. BarDisplay показывает содержимое текущего такта
2. Пользователь может переключаться между тактами
3. Кнопки навигации обновляют свое состояние
4. Отображается информация о текущем такте

### 5. Воспроизведение
1. При нажатии playBtn запускается автоматическое воспроизведение
2. Каждые 2 секунды происходит переход к следующему такту
3. При достижении конца происходит возврат к началу
4. Пользователь может остановить воспроизведение

## Технические особенности

### Модульность
- Каждый компонент имеет четко определенную ответственность
- Слабая связанность между модулями
- Возможность независимого тестирования компонентов

### Расширяемость
- Легкое добавление новых типов аккордов
- Поддержка различных форматов ввода
- Возможность добавления новых типов длительностей

### Производительность
- Ленивая загрузка компонентов
- Эффективное управление DOM
- Кэширование вычислений аккордов

### Совместимость
- Поддержка современных браузеров
- Адаптивный дизайн
- Обратная совместимость с существующим кодом

## Установка и запуск

### Требования
- Node.js 16+
- npm или yarn

### Установка зависимостей
```bash
npm install
```

### Разработка
```bash
# Запуск в режиме разработки с автоперезагрузкой
npm run dev

# Сборка CSS
npm run build:css

# Генерация документации
npm run docs
```

### Сборка для продакшена
```bash
# Полная сборка приложения
npm run build
```

## Использование

### Веб-интерфейс
1. Откройте `index.html` в браузере
2. Введите аккорды в поле "Аккорды" (например: "Am F C G")
3. Выберите количество стрелочек (4, 8, 16)
4. Настройте темп (BPM)
5. Выберите шаблон боя из выпадающего меню
6. Нажмите "Случайный бой" для генерации паттерна

### Программное использование
```javascript
// Инициализация приложения
const app = GuitarCombatApp.getInstance();
await app.init();

// Создание стрелочек
const arrowDisplay = new ArrowDisplay();
arrowDisplay.init('#beatRow', '#countSelect');
arrowDisplay.setArrowCount(8);

// Создание парсера аккордов
const chordParser = new ChordParser();
chordParser.updateChords("Am F C G", 8);

// Создание такта
const bar = new Bar(0, 8);
bar.setBeatType(0, 1); // первая доля - играть
bar.addChordChange("Am", 0, 8); // аккорд Am на весь такт

// Отображение
const barDisplay = new BarDisplay();
barDisplay.init('#bar-container', '#bar-info');
barDisplay.setBars([bar]);
```

### Работа с шаблонами
```javascript
// Загрузка шаблона
fetch('templates/Пятерка.json')
  .then(response => response.json())
  .then(template => {
    // Применение настроек из шаблона
    app.handleBeatCountChange(template.count);
    app.handleBpmChange(template.bpm);
    
    // Применение паттерна боя
    template.beats.forEach((beat, index) => {
      bar.setBeatType(index, beat);
    });
  });
```

### Экспорт и импорт
```javascript
// Экспорт настроек
const config = {
  settings: app.getState().settings,
  arrows: arrowDisplay.exportConfig(),
  bars: app.bars.map(bar => bar.toJSON())
};

// Сохранение в файл
const dataStr = JSON.stringify(config, null, 2);
const dataBlob = new Blob([dataStr], {type: 'application/json'});
// ... логика сохранения файла
```

## Текущее состояние проекта

### ✅ Реализованные функции
- ✅ Визуализация стрелочек боя с интерактивностью
- ✅ Система аккордов с поддержкой сложных типов
- ✅ Парсинг и валидация аккордов
- ✅ Готовые шаблоны боев
- ✅ Адаптивное отображение (4, 8, 16 стрелочек)
- ✅ Настройки темпа (BPM)
- ✅ Сохранение/загрузка в localStorage
- ✅ Модальные окна для документов
- ✅ Современный UI с темной темой
- ✅ Система сборки с Webpack
- ✅ Генерация документации JSDoc

### 🚧 В разработке
- 🚧 Аудио генерация аккордов
- 🚧 Синхронизация с метрономом
- 🚧 Импорт/экспорт композиций в JSON

### 📋 Планируемые функции
- 📋 Визуализация аппликатуры аккордов
- 📋 Система уроков и прогресса
- 📋 Социальные функции (обмен композициями)
- 📋 Интеграция с Telegram WebApp
- 📋 Мобильное приложение
- 📋 Офлайн режим (PWA)

### 🔧 Технические улучшения
- 🔧 TypeScript для лучшей типизации
- 🔧 Unit тесты для всех компонентов
- 🔧 E2E тесты для пользовательских сценариев
- 🔧 Оптимизация производительности
- 🔧 Кэширование шаблонов
- 🔧 Система плагинов для расширения функциональности

## Технические детали

### Зависимости
- **Tailwind CSS** - для стилизации и адаптивного дизайна
- **Webpack** - для сборки и модульности
- **Babel** - для транспиляции ES6+ в совместимый код
- **JSDoc** - для генерации документации

### Браузерная совместимость
- Chrome 80+
- Firefox 75+
- Safari 13+
- Edge 80+

### Производительность
- Ленивая загрузка компонентов
- Оптимизированная работа с DOM
- Кэширование вычислений аккордов
- Минификация и сжатие ресурсов

## Вклад в проект

### Как внести вклад
1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Стандарты кода
- Используйте ESLint для проверки кода
- Добавляйте JSDoc комментарии для новых методов
- Тестируйте изменения в разных браузерах
- Обновляйте документацию при изменении API

## Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## Контакты

- **Автор**: Dmitrij
- **Проект**: Guitar Combat
- **Версия**: 1.0.0

## Заключение

Guitar Combat представляет собой современную модульную архитектуру для создания интерактивных уроков игры на гитаре. Приложение объединяет визуализацию гитарного боя, работу с аккордами и текстами песен в едином удобном интерфейсе. 

Четкое разделение ответственности между компонентами обеспечивает легкость разработки, тестирования и расширения функциональности. Проект готов к использованию и дальнейшему развитию.
